<div class="headline-play">
<h2><%= @tour.title%></h2>
</div>
<div class ="button-play">
<%= link_to "back", tour_path(@tour), class: "category-link #{'category-link'}" %>
</div>
<div class="audio-file">
<%= audio_tag('', controls: true) %>
</div>

<div
  id="map"
  style="width: 100%;
  height: 300px;"
  data-markers="<%= @markers.to_json %>"
></div>



<!-- Tour completed: Loop through tour, same as show page to check all the status for all the attractions.
Return  -->

<!-- sweet alert when tour is finished -->
<!-- Attraction given, find step status -->

<!-- tour_attraction = tour.tour_attractions.find_by(attraction_id:attration.id)
user_tour = current_user.user_tours.find_by(tour_id:tour.id)
step = step.find_by(user_tour_id: user_tour.id, tour_attraction_id: tour_attraction.id)
step.update(status:"completed") -->